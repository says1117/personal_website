node_modules
dist
dist-ssr
*.local
.env
.env.*
.DS_Store
.vercel

# Private-use game images (Valve IP) — never commit/deploy these.
# The page falls back to original vector art without them.
public/oldgods/*
!public/oldgods/README-oldgods.md
